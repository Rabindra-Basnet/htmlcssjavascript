// stock with the kearning

// loops 

for (let row = 0; row < 5; row++) {
    let space =" ";
    let num = 1;
    for(let cols = 0; cols <= (row); cols++){
        space+= num;
        num++;

    }
    console.log(space);
}